# Copilot Instructions

- Read `AGENTS.md` first and follow it as the authoritative repo guide.
- Keep all code/comments/docs in English.
- Preserve scaffold/bundle invariants: scaffold file is `graft.json`; bundle emits `schema.json` and `metadata.json`.
- Keep API flows aligned with backend constraints (master-key-only push/asset endpoints).
- Run targeted vitest + type-check for touched code before finishing.
